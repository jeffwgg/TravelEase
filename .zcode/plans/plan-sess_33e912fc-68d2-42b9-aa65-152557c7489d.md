## Implementation Plan: Module 7 Cross-Module Reporting + Module 5 Assistance Changes (final)

Same as previously presented, with your addition:
- **Mock data scripts saved for reuse**: new directory `server/supabase/seed/` containing the full seeding SQL split into logical, re-runnable scripts (`01_users.sql`, `02_assistance.sql`, `03_queue.sql`, `04_communication.sql`, `05_metrics.sql`) plus a `README.md` explaining what each does and how to run them. The scripts are executed against your Supabase project now, and stay in the repo for future reuse.

### Summary of work
1. **DB schema** (via Supabase MCP + saved as `server/supabase/migrations/004_analytics_reporting.sql`): `venue_zones.map_x/map_y`, `assistance_requests.acknowledged_at`, new `sla_configs` and `generated_reports` tables with RLS mirroring existing patterns.
2. **Seed mock data** (~6 months): ~500 assistance requests, ~200 issue reports, ~350 queue numbers + events, ~150 communication sessions + ~1,200 messages, ~80 demo auth users + profiles, extra staff, zone coordinates, SLA config, regenerated daily metrics. Existing real rows preserved. Scripts stored in `server/supabase/seed/`.
3. **Web**: AnalyticsPage de-mocked (real KPIs, SVG spatial heatmap from zone coords, zone breakdown with low-sample protection, hotspot correlation table); ServicePerformancePage wired (response/resolution KPIs, SLA breaches, satisfaction trend); NEW UsageInsightsPage (users/adoption, queue analytics, communication usage); ReportGenerationPage real export (CSV + PDF via jspdf, anonymized, preview, saved to generated_reports with re-download); acknowledged_at set on first staff action; fix hardcoded 'Ahmad Khan'.
4. **Docs**: `docs/FR-changes-module5-module7.md` (M7-04/08/15 amended, M7-10/17 supported, M7-11/12 implemented, M7-16 removed, M7-18 descoped, new FR-M7-19..23, Module 5 identity deferred).
5. **Verify**: advisors, KPI test queries, npm build, dev server check, valid CSV/PDF downloads.